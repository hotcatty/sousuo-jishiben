# 搜搜记事本

小红书搜索时用的记事本 Chrome 插件。带着目的搜，贴有用的，跑偏了再拉回来。

仓库内部目录名仍是 `focus-guard`，对外名称以 `manifest.json` 为准。

## 安装

`chrome://extensions/` → 开发者模式 → 加载已解压 → 选本目录。改代码后点刷新，再刷新小红书页。

侧栏走 Chrome Side Panel：点工具栏图标打开。首次打开必须跟在一次用户点击后面。

完整走查见 [`docs/测试须知.md`](docs/测试须知.md)。
